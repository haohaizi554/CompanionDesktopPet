# CompanionDesktopPet v1.2.0 Hardening Implementation Plan

**Goal:** 修复两项 P1、五项 P2，补齐四组回归测试，并产出可验证的 v1.2.0 Windows 单文件发行版。

**Constraints:** 不新增功能或依赖；不改变 52,132 / 533 / 51,326 人格语料计数、隐私策略、既有发布门禁或 README 隐私文案；每个 P1/P2/TEST 项独立提交；所有行为修改遵循红灯—绿灯。

## 1. P1-1: DialogueScheduler 线程安全随机数

- 修改 `tests/CompanionDesktopPet.Tests/DialogueSchedulerTests.cs` 及相关调用测试，先以无参构造 API 形成红灯。
- 修改 `src/CompanionDesktopPet/Services/DialogueScheduler.cs`，移除注入的 `Random`，改用 `Random.Shared`。
- 修改 `src/CompanionDesktopPet/MainWindow.xaml.cs`、`src/CompanionDesktopPet/UI/MainWindowOptions.cs` 和所有调用方。
- 运行调度器与窗口相关测试；提交 `fix(p1-1): ...`。

## 2. P1-2: SafeFeedback 有效日历覆盖

- 增加 `SafeFeedbackCoverage_DoesNotCrashOnFarFutureDates`，并增加覆盖日期必须落在 2025–2030 的契约测试。
- 将 `src/CompanionDesktopPet/Services/SceneScheduler.SafeFeedback.cs` 的固定矩阵改为受 `ChineseLunisolarCalendar` 支持、仍覆盖 16 种上下文组合的日期。
- 运行 `SceneCatalogSafetyTests`；提交 `fix(p1-2): ...`。

## 3. P2-1: BubbleCountdownController UI 线程契约

- 在类 XML 文档中声明仅限 UI dispatcher 使用。
- 在 Show/Enter/Leave/TryExpire/Suspend/Resume/Hide/Close 入口增加 Debug UI 线程断言。
- 运行控制器测试；提交 `chore(p2-1): ...`。

## 4. P2-2: 菜单模板默认样式隔离

- 先增加样式 setter 契约测试并验证红灯。
- 为 `KawaiiMenuItemStyle` 与 `KawaiiSeparatorStyle` 增加 `OverridesDefaultStyle=True`。
- 运行样式和高对比度测试；提交 `fix(p2-2): ...`。

## 5. P2-3: TreeWeights 单一事实来源

- 在 C# 与 Python 中增加树权重键集、值和总和测试并验证红灯。
- 将 `tree_weights` 加入 persona contract/schema/loader，扩展 C# 生成器并重新生成 `PersonaContract.Generated.cs`。
- 让 `DialogueForest.TreeWeights` 直接引用生成值，刷新受 contract 字节哈希影响的派生配置/报告。
- 运行定向 C#、Python 与生成器检查；提交 `chore(p2-3): ...`。

## 6. P2-4: PetActionCoordinator 防御分支测试

- 增加 `BeginLandingOutsideDragging_IsIgnored` 与 `CompleteWithMismatchedState_IsIgnored`。
- 运行测试；提交 `test(p2-4): ...`。

## 7. P2-5: 过时的本地时间调度 API

- 先增加反射测试，要求无参 `NextDelay()` 带指定中文 `ObsoleteAttribute`。
- 添加属性并检索全部调用方，确保生产路径使用显式时间重载。
- 运行调度器测试；提交 `chore(p2-5): ...`。

## 8. TEST-1: FullscreenStateTracker 状态转换

- 增加四个指定名称测试，覆盖 Unknown→Fullscreen、Unknown→Windowed、重复值和 Unknown 保持。
- 运行定向测试；提交 `test(test-1): ...`。

## 9. TEST-2: CharacterState 核心状态契约

- 新建 `CharacterStateTests.cs`，覆盖时间边界、场景增量、深拷贝、防御性集合和 JSON 往返。
- 运行定向测试；提交 `test(test-2): ...`。

## 10. TEST-3: SceneHistory 索引与容量

- 新建 `SceneHistoryTests.cs`，覆盖 Record、冷却、配额、Restore 快照、空集和 2,000 容量。
- 运行定向测试；提交 `test(test-3): ...`。

## 11. TEST-4: DialogueEventPolicy 全枚举矩阵

- 新建 `DialogueEventPolicyTests.cs`，表驱动覆盖全部 `CompanionEvent`。
- 为 SizeChanged/PositionRestored 保持 direct feedback 的原因补注释。
- 运行定向测试；提交 `test(test-4): ...`。

## 12. 发布门禁、打包与中文 Release

- 运行完整 .NET、Python、配置/生成器、Release 零警告和发布验证门禁；复核固定语料计数与 README 隐私文案未漂移。
- 更新发布清单的 v1.2.0 区段，写入实测数量、EXE SHA-256 和哈希表。
- 打包 v1.2.0 单文件 EXE，验证版本、冷启动与资产完整性。
- 合并到 `main`，通过 `127.0.0.1:7890` 推送 tag/commit，创建正文为中文的 GitHub Release 并上传资产。
