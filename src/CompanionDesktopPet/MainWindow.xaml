<Window x:Class="CompanionDesktopPet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="角色桌宠"
        Width="420"
        Height="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
  <Grid x:Name="InteractionSurface">
    <StackPanel x:Name="SpeechBubble"
                Width="276"
                Margin="12,8,12,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Visibility="Collapsed"
                Panel.ZIndex="2">
      <Border Padding="18,13"
              CornerRadius="25"
              Background="{StaticResource CreamBrush}"
              BorderBrush="{StaticResource RoseBrush}"
              BorderThickness="2">
        <Border.Effect>
          <DropShadowEffect Color="#663A2026"
                            BlurRadius="14"
                            ShadowDepth="3"
                            Opacity="0.32" />
        </Border.Effect>
        <TextBlock x:Name="SpeechText"
                   Foreground="{StaticResource CocoaBrush}"
                   FontFamily="Microsoft YaHei UI"
                   FontSize="15"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   TextAlignment="Center" />
      </Border>
      <Path Width="20"
            Height="12"
            HorizontalAlignment="Center"
            Fill="{StaticResource BlushBrush}"
            Stroke="{StaticResource RoseBrush}"
            StrokeThickness="2"
            Data="M 0,0 L 20,0 L 10,12 Z" />
    </StackPanel>

    <Image x:Name="PetImage"
           Width="320"
           Margin="0,72,0,0"
           VerticalAlignment="Bottom"
           HorizontalAlignment="Center"
           Source="pack://application:,,,/CompanionDesktopPet;component/Assets/character.png"
           Stretch="Uniform"
           RenderTransformOrigin="0.5,0.92">
      <Image.RenderTransform>
        <TransformGroup>
          <ScaleTransform x:Name="BreathingScale" ScaleX="1" ScaleY="1" />
          <ScaleTransform x:Name="ReactionScale" ScaleX="1" ScaleY="1" />
          <RotateTransform x:Name="SwayRotation" Angle="0" />
          <RotateTransform x:Name="ReactionRotation" Angle="0" />
          <TranslateTransform x:Name="FloatingOffset" Y="0" />
        </TransformGroup>
      </Image.RenderTransform>
      <Image.ContextMenu>
        <ContextMenu>
          <MenuItem x:Name="SayMenuItem" Header="说句话 ♡" />
          <MenuItem x:Name="PauseMenuItem" Header="暂停动画" />
          <Separator />
          <MenuItem Header="大小">
            <MenuItem x:Name="SmallSizeMenuItem" Header="小巧" Tag="Small" />
            <MenuItem x:Name="NormalSizeMenuItem" Header="刚刚好" Tag="Normal" IsChecked="True" />
            <MenuItem x:Name="LargeSizeMenuItem" Header="大一点" Tag="Large" />
          </MenuItem>
          <MenuItem x:Name="TopmostMenuItem"
                    Header="保持置顶"
                    IsCheckable="True"
                    IsChecked="True" />
          <MenuItem x:Name="RestorePositionMenuItem" Header="回到右下角" />
          <Separator />
          <MenuItem x:Name="ExitMenuItem" Header="先休息啦（退出）" />
        </ContextMenu>
      </Image.ContextMenu>
    </Image>
  </Grid>
</Window>
